import { ReferenceData } from "@/components/admin/ReferenceData";

export const metadata = { title: "Maʼlumot bazasi — Tarbion administrator" };

export default function AdminReferencePage() {
  return <ReferenceData />;
}
