import { AdminDashboard } from "@/components/admin/Dashboard";

export const metadata = { title: "Bugungi ishlar — Tarbion administrator" };

export default function AdminHomePage() {
  return <AdminDashboard />;
}
