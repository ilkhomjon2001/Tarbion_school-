import { AuditLog } from "@/components/admin/AuditLog";

export const metadata = { title: "Audit jurnali — Tarbion administrator" };

export default function AdminAuditPage() {
  return <AuditLog />;
}
