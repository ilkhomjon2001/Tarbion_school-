import { AdminProfile } from "@/components/admin/AdminProfile";

export const metadata = { title: "Profil — Tarbion administrator" };

export default function AdminProfilePage() {
  return <AdminProfile />;
}
