import { UsersBoard } from "@/components/admin/UsersBoard";

export const metadata = { title: "Foydalanuvchilar — Tarbion administrator" };

export default function AdminUsersPage() {
  return <UsersBoard />;
}
