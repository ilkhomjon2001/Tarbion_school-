import { SettingsBoard } from "@/components/admin/SettingsBoard";

export const metadata = { title: "Sozlamalar — Tarbion administrator" };

export default function AdminSettingsPage() {
  return <SettingsBoard />;
}
